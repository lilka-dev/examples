# Hello World — Keira DynApp Example

The simplest DynApp example that displays "Hello Keira!" on screen.

## Build

```bash
cmake -DCMAKE_TOOLCHAIN_FILE=xtensa-esp32s3.cmake .
make
```

## Usage

Copy `hello.so` to SD card and open from Keira file manager.

Press **B** to exit.
